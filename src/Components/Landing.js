import { Link } from "react-router-dom";
import Header from "./Header";

export default function Landing() {

    return (
        <div>
            <Header active={0}/>
            Landing Page
        </div>
    )
}